export function Background () {
    return (
      <div className="absolute top-0 left-0 w-full h-full pointer-events-none z-0">
        {/* Global floating shapes removed as per user request to prefer section-specific gradients */}
      </div>
    )
}